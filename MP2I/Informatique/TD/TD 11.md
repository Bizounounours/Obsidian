
# Exercice 1

Définition ABR : 
- un ABR est 
		soit vide 
		soit son sous arbre gauche est un ABR dont pour toutes étiquettes $x_{g} < racine:r$
		et son sous arbre droit est un ABR dont pour toutes étiquettes $x_{d} > racine:r$

``` Ocaml
let est_abr a = 
	let rec aux a = match a with
		|Vide -> true,None,None
		|N(x,g,d) -> let okg,ming,maxg = aux g 
					 and okd,mind,maxd = aux d
					 in 
					 let res = okg && okd && mind > x && maxg < x
					 in 
					 res, if g = V then x else ming
						  if d = V then x else maxd
	in
	let resf,_,_ = aux a in resf
```




# Exercice 2

1) tab déja definition
   Sinon trie le tableau: $O(n\log_{2}n)$ tri fusion (dans tous les cas)
   
   on accède à l'élément de rang k `tab.(k-1i)` ou `tab[k-1]` $O(1)$
   
   >[!warning] Remarque : on ne peut faire mieux que $O(n\log_{2}n)$ par un tri procédent par comparaison

1) Elt de rang 1 d'un ABR : il s'agit de l'elt le plus petit. (aller toujours àgauche jusqu'a ce qu'il n'y ait plus de sous arbre gauche)
   
   ```Ocaml
   let rec rang1 a = match a with 
	   |Vide -> failwith "Arbre vide"
	   |N(x,Vide,_) -> x
	   |N(x,g,_) -> rang1 g
   ```
   